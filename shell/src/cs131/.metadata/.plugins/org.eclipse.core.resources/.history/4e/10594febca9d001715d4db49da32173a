package cs131.pa1.filter.sequential;

import java.io.BufferedWriter;
import java.io.FileWriter;

import cs131.pa1.filter.Message;

public class Redirect extends SequentialFilter {
	private final String command;
	public Redirect(String command) {
		this.command = command.trim();
	}
	
	@Override
	public void process() {
		String[] tokenized = command.split(" ");
		if (tokenized.length != 2) {
			System.out.print(Message.REQUIRES_PARAMETER.with_parameter(command));
			return;
		}
		try (BufferedWriter writer = (new FileWriter (tokenized[1]))) {
			
		} catch (Exception e) {
			e.printStackTrace();
		}

	    for (String s : input) {
	        writer.write(s);
	    }
	    writer.close();
		
	}

	@Override
	protected String processLine(String line) {
		
		
		return null;
	}

}
